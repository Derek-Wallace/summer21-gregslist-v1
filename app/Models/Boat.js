export class Boat{
  constructor(make, model, type, price, length, img){
    this.make = make
    this.model = model,
    this.type = type,
    this.price = price,
    this.length = length,
    this.img = img
  }
}